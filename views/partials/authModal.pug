div#authModal.modal.fade(tabindex='-1' role='dialog')
    div.modal-dialog(role='document')
        div.modal-content
            div.modal-header
                h5.modal-title 로그인이 필요합니다.
                button(type='button' class='close' data-dismiss='modal' aria-label='Close')
                    span(aria-hidden='true') &times;
            div.modal-body
                p 로그인이 필요한 기능입니다. 로그인 후 이용해 주세요.
            div.modal-footer
                a(href='/users/login' class='btn btn-primary') 로그인
                button(type='button' class='btn btn-secondary' data-dismiss='modal') 취소